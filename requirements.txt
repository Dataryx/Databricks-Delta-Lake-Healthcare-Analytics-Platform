# Pinned runtime deps for a quick local install.
# Prefer: pip install -e ".[dev,ml]" (reads pyproject.toml).

pyspark==3.5.5
delta-spark==3.2.1
pyyaml==6.0.2
python-dotenv==1.0.1
jsonschema==4.23.0
cryptography==43.0.3
typing-extensions==4.12.2
