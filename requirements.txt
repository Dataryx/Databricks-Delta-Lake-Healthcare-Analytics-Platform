# Runtime dependencies for local demo (PySpark + Delta Lake).
# Prefer `pip install -e ".[dev]"` which installs from pyproject.toml.
# Hashed lockfile generation is documented in docs/reproducibility.md (Phase 7+).

pyspark==3.5.5
delta-spark==3.2.1
pyyaml==6.0.2
python-dotenv==1.0.1
jsonschema==4.23.0
cryptography==43.0.3
typing-extensions==4.12.2
